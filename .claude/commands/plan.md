---
description: Paige turns an APPROVED discovery package into Feature>Epic>Story>Task in ClickUp plus a release plan
argument-hint: <project name> [goal, if skipping discovery for small work]
---

Act as Ben. Planning request: $ARGUMENTS

1. GATE (both required): discovery package CEO-approved (idea file status
   `approved`) AND a shadow-board report exists with no unresolved
   CHALLENGE (/shadow-review). No shadow report, no plan. Small/obvious work may skip discovery ONLY if the CEO says
   so explicitly here.
2. Dispatch Paige (planner) with the discovery package. She produces the
   Feature > Epic > Story > Task decomposition with the ID convention
   (PREFIX-NNN), dependencies, and the release plan
   (templates/release-plan.md -> <project>/planning/ + vault).
3. Review her tree, then show the CEO the FULL set BEFORE any ClickUp write.
4. On approval: create the ClickUp structure exactly as Paige specified
   (epics as parent tasks, stories as subtasks, criteria in descriptions,
   department tags, dependencies linked, feature tags).
5. Finish with the allocation table: story -> named agent -> release ->
   order. Femi-1/Femi-2 flagged for parallel-safe stories.
