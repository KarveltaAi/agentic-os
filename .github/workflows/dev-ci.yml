name: dev-ci
on:
  push:
    branches: [dev]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test 2>&1 || echo 'tests not configured yet'
      - run: echo 'Deployment to dev environment would happen here'
