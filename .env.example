# Copy to .env (gitignored) and fill in. Never commit .env.
OPENROUTER_API_KEY=
# OPTIONAL. HeyGen talking video already works via OpenRouter. Add this only for
# HeyGen-direct extras: your own avatars, cloned voices. app.heygen.com -> Settings -> API
HEYGEN_API_KEY=
