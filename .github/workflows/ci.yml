name: ci
# Informational only. Solo flow: nothing is gated on this check.
on:
  push:
    branches: [dev, main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test 2>&1 || echo 'tests not configured yet'
