# Improvement Log

Every OS upgrade lands here via /retro. Newest first.

| Date | Change | Evidence that prompted it | Expected effect |
|---|---|---|---|
| 2026-09-25 | All 45 role-agent .md files + 6 department-manager .md files: removed "write the vault completion note on completion" instruction from role agents and "enforce the completion-note rule" from managers. Role agents now report findings back to whoever dispatched them; the canonical note is written once, by Ben, after task-validator PASS (matches CLAUDE.md/build.md's existing step-6 ordering, which the agent-level instruction had been silently contradicting) | DEM-002 (2 REWORK + 1 FAIL), DEM-004 (3/3 loops exhausted, escalated to CEO), DEM-007 (3 manager-review loops) — in all three, every rework cycle beyond the first was the completion note self-contradicting (stale/missing/miscounting its own history), never the underlying deliverable. DEM-009/DEM-012 (2026-09-25), where the note was written once at Close per the correct ordering, had zero completion-note-related rework | fewer avoidable rework loops workspace-wide, not just Development — watch DEM-013/015/017 and the next non-DemoProject project's first build session for a repeat |
| 2026-09-25 | ADD skills/dev-build-verification/SKILL.md; EDIT backend-dev.md, fullstack-dev.md, uiux-dev.md (dropped branch/commit/test-run claims, added explicit "you have no shell access" note), dev-manager.md (branch-naming check now Ben's job, not assumed done); EDIT .claude/commands/build.md to require this skill between steps 3-4 for Development stories and move branch/commit to step 6 (Close, post-PASS) | 4/4 Development-department stories across two sessions (DEM-005, DEM-007, DEM-009, DEM-012) hit "no Bash tool available" despite backend-dev.md/fullstack-dev.md instructing them to branch and commit; Ben absorbed the git/build work ad hoc every time with no standing procedure | Ben runs install/build/smoke-test as a mandatory step, not a rediscovery; avoids the parallel-instance git/npm race that granting Bash to Femi-1/Femi-2 simultaneously would risk |
| 2026-09-25 | EDIT .claude/commands/build.md: added a parallel-dispatch contract rule — pin exact literal field/object names in both dispatch prompts when two stories share an API/data contract, don't let each agent re-derive names from prose independently | DEM-007 (Ada and Bayo independently named the same DB index differently) and DEM-009/DEM-012 (Bayo and Femi-2 independently named the honeypot field `website` vs `company` and the consent field `consent` vs `waitlistConsent`) — same failure mode, two months apart, caught at integration/review both times | fewer contract-reconciliation passes on future parallel-dispatched story pairs |
| 2026-09-23 | Rolled back v5 Gitflow approvals: removed dev/staging rulesets, main keeps only no-force-push + no-delete; dropped staging/main-deploy workflows; /gitflow is now a solo dev→main fast-forward | release stuck: rulesets required 1 approving review and GitHub blocks self-approval, so all PRs were permanently BLOCKED | a solo operator can ship; Vera's PASS stays the quality gate |
| 2026-07-11 | v2.1: client isolation, skills library, /retro, /backup, cost policy | design review of CEO concerns | agency-safe, portable, self-improving |
